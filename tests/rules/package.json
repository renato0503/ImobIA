{
  "name": "imobia-rules-tests",
  "private": true,
  "version": "0.1.0",
  "description": "Testes das regras do Firestore via emulador",
  "scripts": {
    "test": "node test-rules.mjs"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.12.0"
  }
}
